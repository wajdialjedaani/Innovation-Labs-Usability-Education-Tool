import styles from "@/styles/stats.module.scss";

export default function AllData({data}) {
    return (
        <div className={`col-12 p-3 ${styles.statsContainer}`}>
            <h2 className={`h2 ${styles.graphTitle}`}>All data</h2>
            {
                // show data
            }
        </div>
    )
}